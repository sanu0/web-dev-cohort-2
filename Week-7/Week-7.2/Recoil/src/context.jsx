import { createContext } from "react";

export const CountContext = createContext(0);

//this is now we have created a teleporter and now this will help
/**In App.jsx
 * 
 * Wrap any component that wants to use the teleported value inside a provider
 */