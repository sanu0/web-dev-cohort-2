/**
 * In react you cannot return multiple top level xml, you have to bind it under a parent div to return it in react.
 * You can't return multiple tags or divs in react as it makes easy to do reconciliation.
 * 
 * Create a react app that has a 
 * 1. Header component that takes a title as a prop and renders it inside a div.
 * 2. The top level App component renders 2 headers.
 */